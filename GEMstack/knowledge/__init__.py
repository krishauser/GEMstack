"""Models and parameters common to onboard / offboard use.  This module does
not contain too much code, but rather stores configuration files and model
parameters.

The file "current.yaml / "current.py" / "current.pkl" in each directory will
store the current configuration / model being used.
"""